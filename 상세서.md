# FieldGuard AI 상세서

작성일: 2026-05-05  
문서 성격: 구현 기준 문서  
대상: 기획자, UI/UX 디자이너, 프론트엔드 개발자, 백엔드 개발자

## 1. 문서 목적

이 문서는 FieldGuard AI Web App을 실제 구현할 때 기준으로 사용할 화면, 사용자 흐름, 디자인 시스템, 컴포넌트, 상태값, 반응형, 접근성, 예외 처리 명세를 정의한다.

FieldGuard AI는 `진단 확정 서비스`가 아니라 `농업 위험 예측 및 현장 의사결정 보조 서비스`이다. 모든 AI 결과 문구에는 확정 진단, 처방, 방제 지시 표현을 사용하지 않는다.

금지 표현:

- "확진"
- "반드시 방제"
- "이 농약을 사용하세요"
- "병명이 확실합니다"
- "안전합니다"

허용 표현:

- "의심"
- "가능성"
- "위험 상승"
- "공식 자료 확인 필요"
- "전문가 상담 권장"
- "현장 확인 필요"

## 2. 제품 원칙

1. 첫 화면에서 `오늘 위험`, `우선 작업`, `내 필지`, `날씨 요약`을 동시에 확인할 수 있어야 한다.
2. 농민이 매일 반복 사용하는 업무형 UI로 설계한다. 장식적 히어로, 과한 애니메이션, 마케팅형 카피는 사용하지 않는다.
3. 위험도는 색상만으로 표현하지 않는다. 항상 `숫자`, `등급`, `원인 텍스트`를 함께 제공한다.
4. AI 결과는 근거 데이터와 분리해서 표시한다. 공공데이터 원문 또는 공식 링크 접근을 항상 제공한다.
5. 사진 판독 결과는 치료 지시가 아니라 현장 확인 체크리스트로 연결한다.
6. 농약 관련 화면은 공식 안전사용지침 조회 기능으로 제한한다.
7. 모바일에서는 빠른 확인과 체크 완료를 우선하고, 데스크톱에서는 비교와 분석을 우선한다.

## 3. 정보 구조

### 3.1 1차 메뉴

| 메뉴 | 라우트 예시 | 목적 |
|---|---:|---|
| 대시보드 | `/dashboard` | 오늘 위험, 필지, 작업 우선순위 통합 확인 |
| 필지 지도 | `/fields/map` | 필지 위치, 작물, 위험도 지도 확인 |
| 작업 카드 | `/tasks` | 오늘/이번 주 작업 확인 및 완료 처리 |
| 사진 판독 | `/diagnosis` | 병징 사진 업로드, AI 의심 후보, 체크리스트 확인 |
| 자료/상담 패키지 | `/reports` | 상담용 요약 리포트 생성 및 기록 확인 |
| 설정 | `/settings` | 작물, 필지, 알림, 계정 설정 |

### 3.2 주요 객체

| 객체 | 필수 필드 | 설명 |
|---|---|---|
| Field | `id`, `name`, `address`, `lat`, `lng`, `cropName`, `areaM2`, `riskLevel` | 사용자가 등록한 필지 |
| Crop | `name`, `growthStage?`, `registeredAt` | 필지별 재배 작물 |
| WeatherRisk | `precipitation`, `temperature`, `wind`, `humidity`, `summary` | 기상청 데이터 기반 위험 |
| PestRisk | `cropName`, `candidateName`, `score`, `reasons`, `officialSources` | 농사로 및 날씨 기반 병해충 위험 후보 |
| TaskCard | `id`, `priority`, `title`, `reason`, `durationMin`, `checks`, `sources`, `status` | 오늘 실행할 작업 |
| DiagnosisRecord | `id`, `imageUrl`, `cropName`, `candidates`, `checklist`, `confidenceBand`, `createdAt` | 사진 판독 기록 |
| Report | `id`, `fieldId`, `period`, `summary`, `photos`, `tasks`, `risks` | 상담용 패키지 |

## 4. 사용자 흐름

### 4.1 최초 사용자 흐름

1. 사용자가 서비스 접속
2. 계정 생성 또는 게스트 체험 선택
3. 필지 등록 방식 선택
4. 주소, PNU, 지도 클릭, 위도/경도 중 하나로 위치 입력
5. 팜맵 API로 필지 정보 조회
6. 필지명, 작물명, 생육 단계 선택 또는 직접 입력
7. 대시보드로 이동
8. 오늘 위험도와 첫 작업 카드 표시

### 4.2 매일 확인 흐름

1. 사용자가 대시보드 접속
2. 기본 선택 필지의 오늘 위험도 확인
3. 위험 원인과 긴급 작업 카드 확인
4. 필요한 작업을 수행
5. 작업 카드의 체크리스트 완료 처리
6. 작업 완료 이력이 필지 타임라인에 저장

### 4.3 사진 판독 흐름

1. 사용자가 사진 판독 화면 진입
2. 필지와 작물 선택
3. 잎, 줄기, 열매, 밭 전체 중 촬영 부위 선택
4. 사진 업로드
5. Gemini 이미지 분석 실행
6. 의심 후보, 판단 근거, 추가 촬영 안내 표시
7. 농사로 공식 자료와 비교
8. 현장 체크리스트 저장
9. 필요 시 상담 리포트에 포함

### 4.4 농약안전사용지침 확인 흐름

1. 병해충 위험 후보 또는 사진 의심 후보에서 `공식 안전사용지침 확인` 클릭
2. 작물명과 병해충명을 기준으로 농사로 `agchmSafeManualList` 조회
3. 결과 목록 표시
4. 사용자가 항목 선택
5. 수확 전 안전사용기간, 사용횟수, 적용대상, 원문 링크 확인
6. 확인 내역을 상담 패키지에 저장 가능

## 5. 화면별 상세 명세

### 5.1 대시보드

목적: 사용자가 오늘 무엇을 먼저 해야 하는지 10초 안에 파악한다.

데스크톱 구조:

```text
┌──────────────────────────────────────────────────────────────┐
│ 상단바: 오늘 날짜 / 선택 필지 / 작물 / 동기화 상태 / 설정     │
├───────────────┬────────────────────────────┬─────────────────┤
│ 필지 목록      │ 오늘 위험도                 │ 오늘 작업 카드    │
│ + 지도 미리보기 │ 날씨 위험 / 병해충 위험      │ 체크리스트        │
├───────────────┴────────────────────────────┴─────────────────┤
│ 최근 사진 기록 / 농약안전사용지침 / 상담 리포트 요약          │
└──────────────────────────────────────────────────────────────┘
```

필수 영역:

| 영역 | 크기 규칙 | 필수 내용 |
|---|---|---|
| 상단바 | 높이 64px | 오늘 날짜, 선택 필지, 선택 작물, 마지막 갱신 시각 |
| 필지 목록 | 데스크톱 폭 300-340px | 필지명, 작물명, 위험 등급, 면적 |
| 지도 미리보기 | 높이 220px 이상 | 필지 핀, 위험도 색상, 확대 버튼 |
| 오늘 위험도 | 최소 폭 480px | 총점, 등급, 위험 원인 3개 이하 |
| 날씨 위험 | 2x2 또는 3x2 그리드 | 강수, 고온, 저온, 강풍, 습도 |
| 오늘 작업 카드 | 폭 340-400px | 우선순위 1-5위, 완료 체크 |
| 하단 기록 | 높이 180-260px | 사진 4장, 안전사용지침 CTA, 리포트 CTA |

대시보드 표시 규칙:

- 오늘 총 위험도는 0-100 숫자와 등급을 함께 표시한다.
- 위험 원인은 최대 3개만 노출한다. 더보기 클릭 시 전체 목록 표시.
- 작업 카드는 우선순위 높은 순으로 정렬한다.
- 완료된 작업은 당일 목록 하단으로 이동하고 취소 가능 상태를 10초 동안 제공한다.
- 데이터 갱신 시각이 3시간을 초과하면 `정보 갱신 필요` 배지를 표시한다.

예시 문구:

```text
오늘 위험도 78 / 높음
원인: 내일 오전 강수 23mm 예보, 습도 86%, 배추 병해충 발생정보 존재
권장: 오전 중 배수로 점검 후 잎 뒷면 병반 확인
```

### 5.2 필지 등록 화면

목적: 사용자가 자신의 농지를 오류 없이 등록한다.

등록 방식 탭:

| 탭 | 입력 필드 | 사용 조건 |
|---|---|---|
| 주소/지번 | 주소 검색어, 상세 지번 | 주소를 아는 사용자 |
| PNU | 19자리 PNU | 행정 필지 번호를 아는 사용자 |
| 지도 선택 | 지도 클릭 좌표 | 주소가 불명확한 사용자 |
| 좌표 입력 | 위도, 경도 | GPS 좌표를 아는 사용자 |

필드 규칙:

- PNU는 숫자 19자리만 허용한다.
- 위도는 `33.0-39.5`, 경도는 `124.0-132.0` 범위를 기본 허용한다.
- 좌표 범위를 벗어나면 `대한민국 농경지 위치로 확인되지 않습니다.` 오류 표시.
- 팜맵 조회 결과가 2개 이상이면 목록에서 선택하도록 한다.
- 필지명 기본값은 `작물명 + 행정동` 형식이다. 예: `배추 - 해룡면`.

성공 후 표시:

- 필지 면적: `㎡`와 `평` 병기
- 주소 또는 지번
- 중심 좌표
- 팜맵 원천 데이터 갱신일이 있으면 표시

### 5.3 필지 지도 화면

목적: 여러 필지의 위치와 위험도를 비교한다.

데스크톱 구조:

```text
┌───────────────┬──────────────────────────────────────┐
│ 필터 패널      │ 지도                                 │
│ - 작물         │ - 위험도 색상 핀                     │
│ - 위험 등급    │ - 선택 필지 외곽선                   │
│ - 기간         │ - 주변 필지 반경 조회                 │
└───────────────┴──────────────────────────────────────┘
```

지도 규칙:

- 기본 줌은 선택 필지가 화면 중앙에 60% 이상 보이도록 설정한다.
- 위험도 핀은 `낮음`, `주의`, `높음`, `데이터 없음` 4종이다.
- 핀 클릭 시 280px 폭의 요약 패널을 표시한다.
- 지도 위 컨트롤은 우측 상단에 44x44px 아이콘 버튼으로 배치한다.
- 주변 농경지 반경 조회 기본값은 500m, 선택 옵션은 250m, 500m, 1km, 2km이다.

핀 팝오버 내용:

- 필지명
- 작물명
- 오늘 위험도
- 가장 큰 위험 원인 1개
- `상세 보기` 버튼

### 5.4 작업 카드 화면

목적: 오늘과 이번 주 작업을 완료 가능한 단위로 관리한다.

탭:

- 오늘
- 이번 주
- 완료

작업 카드 필수 요소:

| 요소 | 표시 규칙 |
|---|---|
| 우선순위 | `1순위`, `2순위` 형식. 숫자 배지 사용 |
| 작업명 | 20자 이내 우선. 넘치면 2줄까지 표시 |
| 이유 | 데이터 근거 1-2개로 작성 |
| 예상 시간 | `약 20분` 형식 |
| 확인 항목 | 체크박스 3-6개 |
| 근거 데이터 | 접기 영역. API명, 수집 시각, 원문 링크 표시 |
| 완료 버튼 | 체크리스트 1개 이상 체크 후 활성화 |

상태 전환:

```text
대기 → 진행 중 → 완료
대기 → 보류
진행 중 → 보류
완료 → 완료 취소
```

작업 생성 우선순위 기준:

1. 위험도 `높음`이며 24시간 내 실행 필요
2. 강수, 강풍, 고온 등 기상 이벤트가 48시간 내 예보됨
3. 병해충 발생정보와 작물 조건이 일치
4. 주간농사정보의 권장 작업 시기와 일치
5. 사용자가 최근 7일 내 완료하지 않은 반복 작업

### 5.5 사진 판독 화면

목적: 병징 사진에서 의심 후보와 추가 확인 항목을 제공한다.

업로드 조건:

| 항목 | 규칙 |
|---|---|
| 파일 형식 | JPG, PNG, WEBP |
| 파일 크기 | 1장당 최대 10MB |
| 업로드 수 | 1회 최대 5장 |
| 권장 해상도 | 짧은 변 1024px 이상 |
| 필수 선택값 | 필지, 작물, 촬영 부위 |

촬영 부위 옵션:

- 잎 앞면
- 잎 뒷면
- 줄기
- 열매
- 뿌리
- 밭 전체
- 기타

AI 결과 표시 순서:

1. 결과 경고문
2. 의심 후보 최대 3개
3. 후보별 판단 근거
4. 불확실성 원인
5. 추가 촬영 안내
6. 현장 확인 체크리스트
7. 농사로 공식 자료 비교 링크

결과 경고문 고정 문구:

```text
이 결과는 사진과 현재 기상 조건을 바탕으로 한 의심 후보입니다. 병해충 확정 진단이나 농약 처방이 아닙니다.
```

신뢰도 표시:

| 신뢰도 구간 | 표시 문구 | 사용 조건 |
|---|---|---|
| 높음 | 유사 특징이 뚜렷함 | 이미지 품질 양호, 후보 특징 다수 일치 |
| 보통 | 추가 확인 필요 | 일부 특징 일치, 촬영 각도 제한 |
| 낮음 | 판독 제한 | 흐림, 원거리, 병징 불명확, 작물 불일치 |

금지:

- 확률을 소수점으로 노출하지 않는다.
- 단일 후보만 확정적으로 강조하지 않는다.
- 농약명 추천을 자동 생성하지 않는다.

### 5.6 농약안전사용지침 화면

목적: 공식 지침을 빠르게 확인하게 한다.

검색 필드:

- 작물명
- 병해충명 또는 적용대상
- 농약명
- 안전사용기간 필터

목록 컬럼:

| 컬럼 | 표시 규칙 |
|---|---|
| 작물 | 좌측 정렬 |
| 적용대상 | 좌측 정렬 |
| 품목/성분 | 좌측 정렬 |
| 수확 전 사용기간 | 숫자 강조 |
| 사용횟수 | 숫자 강조 |
| 원문 | 외부 링크 아이콘 |

주의 문구:

```text
FieldGuard AI는 농약을 처방하지 않습니다. 실제 사용 전 농사로 공식 지침, 제품 라벨, 전문가 상담을 확인하세요.
```

### 5.7 자료/상담 패키지 화면

목적: 농업기술센터 또는 전문가 상담 전 필요한 자료를 한 번에 정리한다.

리포트 구성:

1. 필지 정보
2. 작물 정보
3. 최근 7일 날씨 요약
4. 최근 7일 위험도 변화
5. 병징 사진 기록
6. 완료/미완료 작업
7. AI 의심 후보와 현장 체크리스트
8. 공식 자료 링크

내보내기:

- PDF
- 인쇄
- 공유 링크

PDF 기본 규격:

- A4 세로
- 여백 16mm
- 본문 10.5pt
- 사진은 최대 2열
- 위험도 그래프는 7일 선형 차트

## 6. 디자인 토큰

### 6.1 색상 팔레트

기본 배경과 업무형 UI 중심으로 사용한다. 위험도 색상은 색약 사용자를 고려해 아이콘과 텍스트를 병기한다.

| 토큰 | HEX | 용도 |
|---|---:|---|
| `color-bg` | `#F6F8F4` | 앱 전체 배경 |
| `color-surface` | `#FFFFFF` | 카드, 패널, 모달 |
| `color-surface-muted` | `#EEF4EF` | 보조 패널 |
| `color-text` | `#17211B` | 기본 텍스트 |
| `color-text-muted` | `#5D6B63` | 보조 텍스트 |
| `color-border` | `#D8E1DA` | 기본 경계선 |
| `color-primary` | `#256D3A` | 주요 버튼, 선택 상태 |
| `color-primary-hover` | `#1D5A30` | 주요 버튼 hover |
| `color-secondary` | `#0F6B7A` | 지도, 링크, 보조 액션 |
| `color-accent` | `#B46A1F` | 중요 근거, 주의 CTA |
| `risk-low` | `#2E7D32` | 낮음 |
| `risk-watch` | `#B7791F` | 주의 |
| `risk-high` | `#C2410C` | 높음 |
| `risk-critical` | `#B91C1C` | 긴급 |
| `risk-unknown` | `#64748B` | 데이터 없음 |
| `info` | `#0E7490` | 정보 |
| `success` | `#15803D` | 완료 |
| `warning` | `#A16207` | 경고 |
| `danger` | `#B91C1C` | 오류 |

사용 규칙:

- 본문 텍스트 대비는 배경 대비 4.5:1 이상이어야 한다.
- 위험도 배지는 색상과 함께 `낮음`, `주의`, `높음`, `긴급`, `데이터 없음` 텍스트를 포함한다.
- 한 화면에서 주요 CTA 색상은 `color-primary` 하나만 사용한다.
- 지도 핀과 차트 색상은 동일한 위험도 토큰을 사용한다.

### 6.2 위험도 등급

초기 MVP 기준값이다. 실제 운영 전 공식 재해 기준과 농업 전문가 검토로 조정한다.

| 점수 | 등급 | 색상 | UI 문구 |
|---:|---|---|---|
| 0-39 | 낮음 | `risk-low` | 평소 관리 |
| 40-69 | 주의 | `risk-watch` | 오늘 확인 필요 |
| 70-89 | 높음 | `risk-high` | 우선 작업 필요 |
| 90-100 | 긴급 | `risk-critical` | 즉시 현장 확인 |
| null | 데이터 없음 | `risk-unknown` | 데이터 확인 불가 |

기상 위험 기본 기준:

| 항목 | 주의 | 높음 | 긴급 |
|---|---:|---:|---:|
| 6시간 강수량 | 5mm 이상 | 20mm 이상 | 50mm 이상 |
| 일 최고기온 | 30도 이상 | 33도 이상 | 35도 이상 |
| 일 최저기온 | 3도 이하 | 0도 이하 | -3도 이하 |
| 풍속 | 8m/s 이상 | 14m/s 이상 | 20m/s 이상 |
| 상대습도 | 80% 이상 | 90% 이상 | 95% 이상 |

### 6.3 폰트 체계

기본 폰트 스택:

```css
font-family: "Pretendard", "Noto Sans KR", "Apple SD Gothic Neo", "Malgun Gothic", system-ui, sans-serif;
```

숫자 표시:

```css
font-variant-numeric: tabular-nums;
```

타입 스케일:

| 토큰 | 크기 | 굵기 | 줄간격 | 용도 |
|---|---:|---:|---:|---|
| `text-display` | 28px | 700 | 36px | 대시보드 핵심 숫자 |
| `text-page-title` | 24px | 700 | 32px | 페이지 제목 |
| `text-section-title` | 18px | 700 | 26px | 섹션 제목 |
| `text-card-title` | 16px | 700 | 24px | 카드 제목 |
| `text-body` | 14px | 400 | 22px | 본문 |
| `text-body-strong` | 14px | 700 | 22px | 강조 본문 |
| `text-meta` | 12px | 500 | 18px | 메타 정보, 출처 |
| `text-caption` | 11px | 500 | 16px | 배지 보조 텍스트 |

규칙:

- 본문 기본 크기는 14px이다.
- 메타 정보는 12px 미만으로 내리지 않는다.
- 버튼 텍스트는 14px 또는 15px만 사용한다.
- 뷰포트 폭에 따라 글자 크기를 자동 확대하지 않는다.
- 자간은 `0`으로 고정한다.

### 6.4 간격과 모서리

| 토큰 | 값 | 용도 |
|---|---:|---|
| `space-1` | 4px | 아이콘과 텍스트 간격 |
| `space-2` | 8px | 작은 내부 여백 |
| `space-3` | 12px | 폼 그룹 간격 |
| `space-4` | 16px | 카드 내부 여백 |
| `space-5` | 20px | 섹션 내부 여백 |
| `space-6` | 24px | 섹션 간격 |
| `space-8` | 32px | 페이지 여백 |

모서리:

| 토큰 | 값 | 용도 |
|---|---:|---|
| `radius-sm` | 4px | 배지, 작은 입력 |
| `radius-md` | 6px | 버튼, 입력창 |
| `radius-lg` | 8px | 카드, 모달 |
| `radius-full` | 999px | 상태 점, 원형 아이콘 |

카드 반경은 8px을 초과하지 않는다.

## 7. 레이아웃 규칙

### 7.1 브레이크포인트

| 구간 | 폭 | 레이아웃 |
|---|---:|---|
| Mobile | 0-767px | 단일 컬럼, 하단 내비게이션 |
| Tablet | 768-1199px | 2컬럼, 접이식 패널 |
| Desktop | 1200-1439px | 3컬럼 대시보드 |
| Wide | 1440px 이상 | 3컬럼 + 하단 확장 패널 |

### 7.2 페이지 컨테이너

| 구간 | 좌우 여백 | 최대 폭 | 그리드 |
|---|---:|---:|---|
| Mobile | 16px | 없음 | 4컬럼 |
| Tablet | 24px | 없음 | 8컬럼 |
| Desktop | 32px | 1360px | 12컬럼 |
| Wide | 40px | 1560px | 12컬럼 |

### 7.3 대시보드 컬럼

데스크톱:

```css
grid-template-columns: 320px minmax(480px, 1fr) 360px;
gap: 16px;
```

와이드:

```css
grid-template-columns: 340px minmax(560px, 1fr) 400px;
gap: 20px;
```

태블릿:

- 상단: 필지 선택 + 오늘 위험도
- 하단: 지도와 작업 카드 2컬럼
- 우측 패널은 하단으로 이동

모바일:

- 상단 요약 카드
- 오늘 작업 카드
- 위험 원인
- 필지 목록
- 최근 기록
- 하단 탭 내비게이션

### 7.4 스크롤 규칙

- 데스크톱 상단바는 고정한다.
- 좌측 필지 목록은 내부 스크롤을 사용한다.
- 모달 내부는 최대 높이 `calc(100vh - 96px)`로 제한한다.
- 모바일에서 지도 화면은 지도 영역을 최소 `60vh`로 유지한다.

## 8. 공통 컴포넌트 규격

### 8.1 버튼

종류:

| 종류 | 높이 | 패딩 | 사용 |
|---|---:|---:|---|
| Primary | 40px | 0 16px | 주요 저장, 등록, 분석 실행 |
| Secondary | 40px | 0 16px | 보조 이동, 상세 보기 |
| Tertiary | 36px | 0 12px | 접기, 더보기 |
| Danger | 40px | 0 16px | 삭제, 연결 해제 |
| Icon | 40x40px | 0 | 지도 확대, 새로고침 |
| Compact | 32px | 0 10px | 카드 내부 작은 액션 |

상태:

| 상태 | 규칙 |
|---|---|
| Default | 지정 색상 사용 |
| Hover | 배경 8% 어둡게 |
| Active | 1px 아래 눌림 효과 |
| Focus | 2px 포커스 링, `#0F6B7A` |
| Disabled | 불투명도 45%, 클릭 불가 |
| Loading | 좌측 spinner, 텍스트 유지 |

버튼 문구 규칙:

- 동사로 시작한다. 예: `필지 등록`, `분석 시작`, `리포트 생성`
- 파괴적 액션은 명확히 표시한다. 예: `필지 삭제`
- 아이콘 단독 버튼은 `aria-label` 필수.

### 8.2 카드

기본 카드:

```css
background: #FFFFFF;
border: 1px solid #D8E1DA;
border-radius: 8px;
padding: 16px;
box-shadow: none;
```

카드 종류:

| 카드 | 필수 요소 | 사용 위치 |
|---|---|---|
| RiskSummaryCard | 위험 점수, 등급, 원인 | 대시보드 |
| WeatherRiskCard | 항목명, 수치, 등급, 시간 | 대시보드 |
| TaskCard | 우선순위, 작업명, 이유, 체크 | 작업 화면 |
| FieldCard | 필지명, 작물, 면적, 위험도 | 필지 목록 |
| DiagnosisCard | 의심 후보, 근거, 신뢰도 | 사진 판독 |
| SourceCard | API명, 수집시각, 원문 링크 | 근거 데이터 |

카드 규칙:

- 카드 안에 별도 카드 형태의 컨테이너를 중첩하지 않는다.
- 반복 목록 카드는 동일 높이를 강제하지 않는다. 단, 작업 카드의 우선순위 배지는 고정 크기 32x32px이다.
- 카드 제목은 최대 2줄로 제한한다.
- 카드 내부 CTA는 하단 우측에 배치한다.

### 8.3 입력창

기본 규격:

| 항목 | 값 |
|---|---:|
| 높이 | 44px |
| 좌우 패딩 | 12px |
| 라벨 위치 | 입력창 위 |
| 라벨 크기 | 13px, 600 |
| 오류 문구 | 12px, danger 색상 |
| 도움말 | 12px, muted 색상 |

검증:

- 필수 입력값은 blur 시점과 제출 시점에 검증한다.
- 형식이 명확한 값(PNU, 좌표, 파일 크기)은 입력 즉시 검증한다.
- 오류 문구는 한 문장으로 작성한다.

예시:

```text
PNU
[ 4613025021100010000 ]
오류: PNU는 숫자 19자리여야 합니다.
```

### 8.4 선택 컨트롤

| 컨트롤 | 사용 조건 |
|---|---|
| Select | 작물, 필지, 기간처럼 목록이 8개 이상 |
| Segmented Control | 오늘/이번 주/완료처럼 2-4개 옵션 |
| Checkbox | 작업 확인 항목 |
| Radio | 등록 방식처럼 상호 배타 옵션 |
| Toggle | 알림 켜기/끄기 |
| Slider | 반경 조회 거리 |

### 8.5 배지

규격:

- 높이 24px
- 패딩 0 8px
- 반경 999px
- 폰트 12px, 600

위험도 배지:

```text
[낮음 24]
[주의 52]
[높음 78]
[긴급 94]
[데이터 없음]
```

### 8.6 모달

사용 조건:

- 삭제 확인
- 분석 취소 확인
- 외부 링크 이동 확인
- 필지 등록 상세 선택

규격:

| 구간 | 값 |
|---|---:|
| 데스크톱 폭 | 480px 기본, 최대 720px |
| 모바일 폭 | 화면 폭 - 32px |
| 반경 | 8px |
| 헤더 높이 | 56px |
| 푸터 높이 | 64px |
| 배경 딤 | `rgba(23, 33, 27, 0.48)` |

모달 규칙:

- ESC로 닫을 수 있어야 한다. 단, 업로드/분석 진행 중에는 확인 모달을 먼저 띄운다.
- 첫 포커스는 제목 또는 첫 입력 필드에 둔다.
- 닫기 버튼은 우측 상단 40x40px 아이콘 버튼이다.

### 8.7 토스트

위치:

- 데스크톱: 우측 하단
- 모바일: 하단 내비게이션 위

유지 시간:

| 유형 | 시간 |
|---|---:|
| 성공 | 3초 |
| 정보 | 4초 |
| 경고 | 5초 |
| 오류 | 사용자가 닫을 때까지 또는 8초 |

예시:

```text
작업이 완료 처리되었습니다. [실행 취소]
```

### 8.8 테이블

사용 위치:

- 농약안전사용지침 목록
- 공식 자료 목록
- 상담 리포트 기록

규칙:

- 숫자는 우측 정렬한다.
- 텍스트는 좌측 정렬한다.
- 헤더는 sticky 처리한다.
- 행 hover 상태는 필수이다.
- 모바일에서는 테이블을 카드 목록으로 변환한다.

## 9. 상태값 정의

### 9.1 공통 비동기 상태

```ts
type AsyncStatus = "idle" | "loading" | "success" | "empty" | "error" | "stale";
```

| 상태 | UI |
|---|---|
| idle | 초기 안내 또는 빈 입력 상태 |
| loading | skeleton 또는 spinner |
| success | 정상 데이터 표시 |
| empty | 빈 상태 안내와 다음 액션 표시 |
| error | 오류 원인과 재시도 버튼 |
| stale | 기존 데이터 표시 + 갱신 필요 배지 |

### 9.2 위험도 상태

```ts
type RiskLevel = "low" | "watch" | "high" | "critical" | "unknown";
```

| 상태 | 점수 | 사용자 문구 |
|---|---:|---|
| low | 0-39 | 평소 관리 |
| watch | 40-69 | 오늘 확인 필요 |
| high | 70-89 | 우선 작업 필요 |
| critical | 90-100 | 즉시 현장 확인 |
| unknown | null | 데이터 확인 불가 |

### 9.3 작업 상태

```ts
type TaskStatus = "pending" | "in_progress" | "done" | "deferred" | "cancelled";
```

| 상태 | UI |
|---|---|
| pending | 흰색 카드, 시작 버튼 |
| in_progress | 좌측 primary 보더, 진행 중 배지 |
| done | success 배지, 체크 완료 표시, 목록 하단 |
| deferred | muted 배지, 보류 사유 입력 |
| cancelled | 목록에서 숨김, 기록에는 유지 |

### 9.4 사진 판독 상태

```ts
type DiagnosisStatus =
  | "ready"
  | "uploading"
  | "analyzing"
  | "needs_more_photo"
  | "completed"
  | "limited"
  | "failed";
```

| 상태 | UI |
|---|---|
| ready | 업로드 영역 표시 |
| uploading | 업로드 진행률 |
| analyzing | 분석 중 메시지, 취소 버튼 |
| needs_more_photo | 추가 촬영 안내 우선 표시 |
| completed | 의심 후보와 체크리스트 표시 |
| limited | 판독 제한 사유 표시 |
| failed | 재시도와 파일 교체 버튼 |

### 9.5 데이터 출처 상태

```ts
type SourceStatus = "connected" | "delayed" | "unavailable" | "rate_limited";
```

| 상태 | UI 문구 |
|---|---|
| connected | 정상 수집 |
| delayed | 응답 지연 |
| unavailable | 공식 데이터 조회 불가 |
| rate_limited | 요청 한도 초과 |

## 10. AI 출력 규칙

### 10.1 Gemini 출력 JSON 기본 구조

사진 판독:

```json
{
  "disclaimer": "확정 진단이 아닌 의심 후보입니다.",
  "candidates": [
    {
      "name": "노균병",
      "confidenceBand": "보통",
      "visualReasons": ["잎 표면 황화", "반점 형태"],
      "weatherReasons": ["최근 고습 조건"],
      "nextChecks": ["잎 뒷면 흰 곰팡이 여부 확인"]
    }
  ],
  "limitations": ["사진 초점이 일부 흐림"],
  "recommendedPhotos": ["잎 뒷면 근접 사진", "포기 전체 사진"],
  "fieldChecklist": ["잎 뒷면 확인", "배수 상태 확인"]
}
```

작업 카드:

```json
{
  "tasks": [
    {
      "priority": 1,
      "title": "배수로 점검",
      "reason": "내일 오전 강수 예보와 병해충 위험 상승",
      "durationMin": 20,
      "checks": ["배수로 막힘 확인", "고랑 물고임 확인"],
      "sources": ["KMA_VilageFcst", "Nongsaro_dbyhsCccrrncInfoList"]
    }
  ]
}
```

### 10.2 AI 문구 제한

- 한 문장은 60자 이내를 우선한다.
- 전문 용어 뒤에는 쉬운 설명을 붙인다. 예: `고습(습도가 높은 상태)`.
- 위험 원인은 최대 3개까지 요약한다.
- 출처 없는 수치 생성 금지.
- 농약명 자동 추천 금지.
- 공식 데이터가 없으면 `확실한 정보 없음`으로 표시한다.

## 11. 예외 처리 기준

### 11.1 API 오류

| 상황 | 사용자 메시지 | UI 액션 |
|---|---|---|
| 팜맵 조회 실패 | 필지 정보를 불러오지 못했습니다. 입력값을 확인하거나 다시 시도하세요. | 재시도, 수동 좌표 입력 |
| 기상청 API 실패 | 현재 날씨 데이터를 불러오지 못했습니다. 마지막 수집 정보를 표시합니다. | stale 데이터 표시 |
| 농사로 API 실패 | 공식 농업 자료를 불러오지 못했습니다. 잠시 후 다시 시도하세요. | 재시도, 원문 링크 |
| Gemini 실패 | AI 분석을 완료하지 못했습니다. 사진을 유지한 상태로 다시 시도할 수 있습니다. | 재시도, 파일 교체 |
| 요청 한도 초과 | 요청이 많아 잠시 대기해야 합니다. | 재시도 비활성화, 대기 시간 표시 |

### 11.2 데이터 없음

| 화면 | 처리 |
|---|---|
| 등록 필지 없음 | `첫 필지를 등록하세요` CTA 표시 |
| 작물 정보 없음 | 작물 직접 입력 유도 |
| 병해충 자료 없음 | `해당 작물의 공식 발생정보가 확인되지 않습니다.` 표시 |
| 작업 카드 없음 | `오늘 우선 작업이 없습니다. 평소 관리 상태입니다.` 표시 |
| 사진 기록 없음 | 업로드 CTA 표시 |

### 11.3 위험한 오해 방지

다음 상황에서는 경고 배너를 표시한다.

- AI 판독 신뢰도 `낮음`
- 농약안전사용지침 조회 결과 없음
- 기상 데이터가 6시간 이상 갱신되지 않음
- 사용자가 결과 화면에서 농약 관련 액션을 클릭함

경고 배너 예시:

```text
공식 자료가 부족해 판단이 제한됩니다. 현장 확인 후 농업기술센터 상담을 권장합니다.
```

### 11.4 파일 업로드 오류

| 오류 | 메시지 |
|---|---|
| 형식 오류 | JPG, PNG, WEBP 파일만 업로드할 수 있습니다. |
| 크기 초과 | 사진 1장은 최대 10MB까지 업로드할 수 있습니다. |
| 너무 낮은 해상도 | 사진이 작아 판독 정확도가 낮을 수 있습니다. 다시 촬영을 권장합니다. |
| 손상 파일 | 파일을 읽을 수 없습니다. 다른 사진을 선택하세요. |

## 12. 접근성 원칙

### 12.1 키보드 접근성

- 모든 버튼, 링크, 입력창, 체크박스는 키보드로 접근 가능해야 한다.
- 포커스 순서는 화면 시각 순서와 일치해야 한다.
- 모달이 열리면 포커스는 모달 내부에 갇혀야 한다.
- ESC로 모달을 닫을 수 있어야 한다.
- 지도 조작 버튼은 키보드 포커스와 `aria-label`을 제공한다.

### 12.2 시각 접근성

- 본문 텍스트 대비는 4.5:1 이상.
- 큰 텍스트 대비는 3:1 이상.
- 위험도는 색상 단독 사용 금지.
- 차트에는 범례와 수치 라벨을 함께 표시한다.
- 터치 대상 최소 크기는 44x44px이다.

### 12.3 스크린리더

필수 aria 예시:

```html
<button aria-label="필지 지도 확대">...</button>
<section aria-labelledby="today-risk-title">...</section>
<div role="status" aria-live="polite">날씨 데이터를 갱신했습니다.</div>
```

동적 상태 알림:

- 분석 시작
- 분석 완료
- 작업 완료
- 오류 발생
- 데이터 갱신 완료

### 12.4 언어

- `html lang="ko"`를 사용한다.
- 날짜 형식은 `2026년 5월 5일 화요일` 또는 `2026-05-05` 중 화면 성격에 맞게 사용한다.
- 단위는 숫자와 붙여 쓴다. 예: `23mm`, `33도`, `14m/s`.

## 13. 반응형 상세 기준

### 13.1 모바일

폭 0-767px:

- 하단 내비게이션 5개 메뉴 사용.
- 상단바 높이 56px.
- 카드 간격 12px.
- 대시보드 핵심 위험 카드가 첫 화면 상단에 위치.
- 지도는 별도 화면에서 전체 폭 사용.
- 작업 카드 체크박스는 한 줄 높이 최소 44px.
- 테이블은 카드 목록으로 변환.

모바일 대시보드 순서:

1. 선택 필지 드롭다운
2. 오늘 위험도 요약
3. 1순위 작업 카드
4. 날씨 위험 요약
5. 병해충 체크리스트
6. 최근 사진
7. 공식 자료 CTA

### 13.2 태블릿

폭 768-1199px:

- 좌측 패널은 280px 고정 또는 드로어.
- 본문은 2컬럼.
- 작업 카드 패널은 우측 또는 하단으로 이동.
- 지도와 위험 카드가 같은 행에 배치 가능.

### 13.3 데스크톱

폭 1200px 이상:

- 3컬럼 대시보드 사용.
- 좌측 필지 목록은 고정.
- 중앙 위험도 영역은 가장 넓게 배치.
- 우측 작업 카드 패널은 sticky 가능.

## 14. 알림 및 기록

### 14.1 알림 유형

| 유형 | 발생 조건 | 문구 예시 |
|---|---|---|
| 강수 전 점검 | 24시간 내 강수 20mm 이상 | 내일 오전 강수 예보가 있습니다. 배수로 점검이 필요합니다. |
| 고온 위험 | 최고기온 33도 이상 | 고온 위험이 높습니다. 관수와 차광 상태를 확인하세요. |
| 강풍 위험 | 풍속 14m/s 이상 | 강풍 예보가 있습니다. 지주시설을 확인하세요. |
| 병해충 위험 | 병해충 점수 70 이상 | 등록 작물의 병해충 위험이 높아졌습니다. 잎 뒷면을 확인하세요. |
| 작업 미완료 | 1순위 작업이 당일 미완료 | 오늘 우선 작업이 아직 완료되지 않았습니다. |

### 14.2 타임라인 기록

기록 단위:

- 위험도 갱신
- 작업 생성
- 작업 완료
- 사진 업로드
- AI 판독 완료
- 공식 자료 확인
- 리포트 생성

타임라인 아이템 필드:

```ts
type TimelineItem = {
  id: string;
  fieldId: string;
  type: "risk" | "task" | "photo" | "diagnosis" | "source" | "report";
  title: string;
  summary: string;
  createdAt: string;
  sourceIds?: string[];
};
```

## 15. 개발 구현 기준

### 15.1 권장 프론트엔드 구조

React 또는 Next.js 기준 권장 구조:

```text
src/
  app/
    dashboard/
    fields/
    tasks/
    diagnosis/
    reports/
  components/
    layout/
    ui/
    risk/
    fields/
    tasks/
    diagnosis/
    sources/
  domain/
    risk/
    weather/
    pest/
    farmmap/
    tasks/
  services/
    farmmapClient.ts
    kmaClient.ts
    nongsaroClient.ts
    geminiClient.ts
  styles/
    tokens.css
```

원칙:

- API 호출 코드는 UI 컴포넌트에 직접 넣지 않는다.
- 위험도 계산은 `domain/risk`에 둔다.
- 공공 API 응답 파싱은 `services` 또는 `domain`에서 명시적으로 처리한다.
- 농사로 재해예방정보는 `frcDsstrPrevntYear`로 연도 목록을 조회하고 `frcDsstrPrevntLst`로 목록을 조회한다.
- AI 출력은 JSON 스키마 검증 후 UI에 전달한다.
- 날짜와 시간은 Asia/Seoul 기준으로 표시한다.

### 15.2 데이터 갱신 주기

| 데이터 | 기본 갱신 |
|---|---:|
| 초단기 실황 | 30분 |
| 초단기 예보 | 30분 |
| 단기 예보 | 3시간 |
| 병해충 발생정보 | 1일 |
| 주간농사정보 | 1일 |
| 농작물재해예방정보 | 1일 |
| 농약안전사용지침 | 7일 캐시 |
| 팜맵 필지 정보 | 등록 시, 수동 갱신 |

### 15.3 성능 기준

| 항목 | 목표 |
|---|---:|
| 대시보드 첫 표시 | 2.5초 이하 |
| 필지 전환 후 주요 정보 표시 | 1.5초 이하 |
| 사진 업로드 시작 반응 | 300ms 이하 |
| AI 분석 대기 안내 표시 | 즉시 |
| 지도 첫 렌더 | 3초 이하 |

성능 규칙:

- 이미지 썸네일은 320px 너비 이하로 생성한다.
- 원본 사진은 분석과 기록용으로 분리 저장한다.
- 지도 라이브러리는 지도 화면 또는 대시보드 진입 시 지연 로드한다.
- 대시보드는 skeleton UI를 사용한다.

## 16. 문구 기준

### 16.1 버튼

| 상황 | 문구 |
|---|---|
| 필지 등록 | 필지 등록 |
| 분석 시작 | 사진 분석 시작 |
| 작업 완료 | 완료 처리 |
| 공식 자료 | 공식 자료 확인 |
| 리포트 생성 | 상담 리포트 생성 |
| 재시도 | 다시 시도 |

### 16.2 빈 상태

| 상황 | 문구 |
|---|---|
| 필지 없음 | 등록된 필지가 없습니다. 첫 필지를 등록하세요. |
| 작업 없음 | 오늘 우선 작업이 없습니다. 평소 관리 상태입니다. |
| 사진 없음 | 아직 기록된 사진이 없습니다. 병징 사진을 업로드하세요. |
| 자료 없음 | 해당 조건의 공식 자료가 확인되지 않습니다. |

### 16.3 오류 문구

오류 문구는 `무엇이 실패했는지`, `사용자가 다음에 할 수 있는 일`을 포함한다.

예시:

```text
날씨 데이터를 불러오지 못했습니다. 마지막 수집 정보를 표시하며, 잠시 후 다시 시도할 수 있습니다.
```

## 17. QA 체크리스트

### 17.1 화면 QA

- 대시보드 첫 화면에 오늘 위험도와 1순위 작업이 보이는가
- 위험도 색상 없이도 등급을 이해할 수 있는가
- 모바일 360px 폭에서 버튼 텍스트가 잘리지 않는가
- 지도 핀 클릭 시 요약 패널이 화면 밖으로 나가지 않는가
- 사진 업로드 실패 시 파일이 사라지지 않는가
- 완료 처리 후 타임라인에 기록되는가

### 17.2 접근성 QA

- Tab 키만으로 주요 기능을 사용할 수 있는가
- 모달 포커스가 외부로 빠져나가지 않는가
- 스크린리더가 분석 완료 상태를 읽는가
- 위험도 배지가 색상 없이도 의미를 전달하는가
- 모든 아이콘 버튼에 접근 가능한 이름이 있는가

### 17.3 데이터 QA

- API 실패 시 stale 데이터와 오류 상태가 구분되는가
- AI 결과에 금지 표현이 포함되지 않는가
- 농약 관련 화면에서 처방 표현이 나오지 않는가
- 출처 없는 수치가 표시되지 않는가
- 데이터 없음 상태가 오류로 오인되지 않는가
